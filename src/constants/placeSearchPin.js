/**
 * Shared Google Earth–style place-search pin (red teardrop).
 * Served from /public so Map3D, Leaflet, and CSS can use the same asset.
 */
export const PLACE_SEARCH_PIN_SRC = `${import.meta.env.BASE_URL}markers/place-search-pin.svg`
